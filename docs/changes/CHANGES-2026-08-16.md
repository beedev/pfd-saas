# Changes — 2026-08-16 (feat/desktop-analyst)

## Desktop app with the analyst (Artha) + in-app scheduler

Goal: run the full production app (analyst included) as the no-Docker desktop
app. New branch `feat/desktop-analyst` = `feat/analyst-agent` + the desktop
scaffold from `feat/desktop-app` (`cf32cdb`). The prod branch is untouched, so
vaspar-pfd Docker rebuilds are unaffected.

**Verified:**
- All **70** migrations (49 core + 21 `agent_*`) apply on PGlite → **116 tables**
  == vaspar-pfd prod. CRUD + dump/restore green.
- Analyst UI (`/investments/analyst`) + APIs (`/api/agent/decisions`,
  `/api/agent/portfolio/equity-curve`) return 200; core app (login, dashboard,
  gold, retirement) all 200.

**OpenAI key (`08d640c`):** the analyst reads `process.env.OPENAI_API_KEY` in 6
places. `electron/main.js` now reads `.env` / `.env.local` (repo root in dev,
`<userData>` when packaged) and forwards `OPENAI_API_KEY` into the Next child's
curated env — same path as `PFD_DB_DRIVER`. Verified the key loads (`sk-proj…`).

**In-app scheduler:** the Docker container drives jobs with an entrypoint 60s
ticker; the desktop has no always-on process, so `main.js` now runs the ticker
itself — after the server is healthy it POSTs `/api/cron/tick` every 60s with a
persisted per-install `CRON_SECRET` (Bearer), cleared on quit. `/api/cron/tick`
self-seeds each user's jobs (`ensureDefaultJobsForAllUsers`) and runs the due
ones: the analyst intraday/daily runs, news ingest, EOD review, self-tune, plus
daily digest / alerts / SIPs. Verified: tick with secret → 200, without → 401;
after demo+personal exist, one tick **dispatched 22 jobs**, the next `0` (they
advance to their IST-anchored times). Runs only while the app is open — daily
jobs fire when their IST window comes around during a session.

Telegram remains off (no bot token forwarded); its sends are stubbed. The
analyst jobs don't need it.
